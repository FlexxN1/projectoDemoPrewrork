const name = ["ricardo", "paola", "prechet"];
const apellido = ["sam", "andrade", "perst"];

function saludarPerosnas(name, apellido){
    this.name = name;
    this.apellido = apellido;
};

for(const i = 0; i < name.lenth, i < apellido.length; i++){
    const nuevoAprendiz = new saludarPersonas(name[i], apellido[i]);
    console.log(nuevoAprendiz);
};
